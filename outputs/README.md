# Outputs

This directory is the default destination for benchmark runs.

Tracked example outputs live under:

- `reference_local_subset_debug/`

Ad hoc runs can use new subdirectories such as:

- `run_2026_05_25/`
- `paper_parity_attempt/`
